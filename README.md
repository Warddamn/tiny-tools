# tiny-tools

Small, local-first tools that AI agents install with one config line and actually use to cut their time and token spend.
Every tool ships in three layers from one codebase: **core library → CLI → MCP server**.

Status: Phase 0/1 in progress. See `PROGRESS.md`.
